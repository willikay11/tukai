// import { Experience } from '@/lib/definitions';
// import { ApiResponse, fetchExperiences } from '@/lib/data';
// import SingleInvitedExperience from '@/app/experiences/components/singleInvitedExperience';

export default async function InvitedExperiences() {
  // const experiences: ApiResponse = await fetchExperiences();

  return (
    <div className="grid grid-cols-1 grid-rows-[400px] gap-x-4 gap-y-8 md:grid-cols-3 2xl:grid-cols-5">
      {/*{experiences.data.results.map((experience: Experience) => (*/}
      {/*  <div key={experience.id} className="cursor-pointer">*/}
      {/*    <SingleInvitedExperience experience={experience} />*/}
      {/*  </div>*/}
      {/*))}*/}
    </div>
  );
}
