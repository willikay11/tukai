import { Inter } from 'next/font/google';
import localFont from 'next/font/local'
export const satoshi = localFont({ src: '../Satoshi-Regular.woff' })

export const inter = Inter({ subsets: ['latin'] });
